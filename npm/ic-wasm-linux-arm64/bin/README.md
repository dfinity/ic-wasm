# Binary Placeholder

This directory should contain the `ic-wasm` binary for Linux ARM64.

## Download

Run from the npm directory:

```bash
cd npm
./scripts/download-binaries.sh 0.9.11
```

Or manually download from:
https://github.com/dfinity/ic-wasm/releases

## Expected File

- `ic-wasm` (executable binary for Linux ARM64)
